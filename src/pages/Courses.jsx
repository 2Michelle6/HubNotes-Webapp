import { Link } from 'react-router-dom';
import { BookOpen, Gamepad2, Plus, Search } from 'lucide-react';
import { useStudyData } from '../data/StudyData';
export default function Courses() { const { courses } = useStudyData(); return <section className="courses-page-container"><header className="courses-top-bar"><h1 className="title-card-box">Your Courses</h1><button className="btn-neo btn-primary"><Plus size={16} /> Add New Course</button></header><div className="course-search"><Search size={17} /><input placeholder="Search course title or code (e.g., CS 101)..." /><button className="tag-pill-neo active">All</button><button className="tag-pill-neo">Computer Science</button><button className="tag-pill-neo">Science</button></div><div className="courses-grid-neo">{courses.map((course) => <article className="course-card-neo" key={course.id}><div className="course-card-top"><span className="course-icon-badge">{course.image ? <img src={course.image} alt="" /> : '📚'}</span><span className="course-code-pill">{course.code}</span></div><h2>{course.title}</h2><p className="course-description">{course.overview}</p><div className="course-stats"><BookOpen size={14} /> {course.notes.length} Notes <span>◈</span> {course.decks.length} Decks ({course.decks.reduce((sum, deck) => sum + deck.cards.length, 0)} cards)</div><div className="course-actions"><Link className="btn-neo btn-orange" to={`/courses/${course.id}`}>View Notes</Link><Link className="btn-neo btn-primary" to={`/courses/${course.id}`}><Gamepad2 size={15} /> Study Decks</Link></div></article>)}<button className="course-card-neo create-course-card-neo"><Plus size={28} /> <strong>Create Course</strong><span>Start a new subject folder to organize your notes & decks</span></button></div></section>; }
